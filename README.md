# Auto Coder Test Repository
This is a simple Go application that prints "Hello Sandbox (Crafting)!" to the console.

## Running the Application

To build and run the application, use the following commands:

```sh
go build main.go
./main
```
